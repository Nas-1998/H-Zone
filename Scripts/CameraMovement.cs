using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraMovement : MonoBehaviour
{
    // private int cameraSpeed = 10;
    void Update()
    {
        // float hMove = Input.GetAxisRaw("Horizontal");
        // transform.position = new Vector3(transform.position.x + hMove*cameraSpeed*Time.deltaTime, transform.position.y, transform.position.z);
        // float vMove = Input.GetAxisRaw("Vertical");
        // transform.position = new Vector3(transform.position.x, transform.position.y + vMove*cameraSpeed*Time.deltaTime, transform.position.z);
    }
}
